# 🧩 مستندات ماژول‌های ERP (Modules Documentation)

این سامانه به ماژول‌های تخصصی زیر تفکیک شده است که هر کدام وظایف مشخصی در اکوسیستم مالی و اداری سازمان بر عهده دارند:

---

## ۱. ماژول داشبورد مدیریتی (`DashboardView`)
- **هدف**: ارائه نمای کلان (Overview) از وضعیت مالی، حجم درخواست‌ها، بودجه‌های مصرف‌شده و نمودارهای تحلیلی.
- **ویژگی‌ها**:
  - کارت‌های آماری مجموع درخواست‌ها، مبلغ کل پرداخت‌ها، درخواست‌های در انتظار و بودجه مصرفی.
  - نمودارهای دایره‌ای و ستونی (`Recharts`) برای توزیع درخواست‌ها بر اساس نوع و وضعیت.
  - جدول دسترسی سریع به آخرین درخواست‌های ثبت شده در سازمان.
- **نقش‌های مجاز**: مدیران ارشد، سرپرستان خزانه‌داری، ادمین.

---

## ۲. ماژول درخواست‌های مالی و پرداخت (`MyRequestsView` & `RequestTableView`)
- **هدف**: ثبت، پیگیری، بررسی، تایید، عودت و پرداخت درخواست‌های وجه (هزینه‌های جاری، پیش‌پرداخت، تنخواه، عودت وجه مشتری).
- **ویژگی‌ها**:
  - فرم ثبت درخواست پیشرفته با انتخاب حساب ذینفع، مبلغ به ریال و حروف (`numberToWords.ts`).
  - کارتابل بررسی و تایید مرحله‌به‌مرحله با نمایش گام‌های تایید (`WorkflowChartView`).
  - قابلیت چاپ رسمی درخواست (`PrintRequestModal`).
- **نقش‌های مجاز**: درخواست‌کنندگان، مدیران شعب، تاییدکنندگان، سرپرست خزانه‌داری.

---

## ۳. ماژول خزانه‌داری و پرداخت گروهی (`BulkPaymentExportModal`)
- **هدف**: مدیریت نهایی تسویه حساب‌ها، انتخاب حساب بانکی مبدا شرکت و تولید فایل‌های تسویه بانکی دسته‌ای.
- **ویژگی‌ها**:
  - انتخاب حساب‌های بانکی شرکت (`CompanyBankAccount`).
  - محاسبه مجموع مبالغ تایید شده آماده پرداخت.
  - خروجی اکسل یا دسته‌ای بانکی (`xlsx`).
- **نقش‌های مجاز**: سرپرست خزانه‌داری (`treasury_executor`)، ادمین.

---

## ۴. ماژول دبیرخانه و نامه‌نگاری داخلی (`LettersView`)
- **هدف**: اتوماسیون اداری برای ثبت نامه‌ها، ارجاع مکاتبات بین واحدها، پیوست فایل، و امضای دیجیتال.
- **ویژگی‌ها**:
  - ثبت نامه جدید با شماره اتوماتیک، موضوع، گیرندگان و متن غنی.
  - سیستم نسخه‌بندی متن نامه (`Version Control`).
  - کارتابل نامه‌های دریافتی، ارسالی، پیش‌نویس‌ها و بایگانی.
- **نقش‌های مجاز**: کلیه پرسنل سازمان با دسترسی `manage_letters`.

---

## ۵. ماژول پشتیبانی و خدمات پس از فروش (`SupportView`)
- **هدف**: پیگیری شکایات و درخواست‌های پشتیبانی مشتریان که نیاز به بررسی مالی یا عودت وجه دارند.
- **ویژگی‌ها**:
  - ثبت پرونده پشتیبانی با مشخصات مشتری و مبلغ مطالبه.
  - تایید مالی مبالغ عودتی (`financial_approve_support`).
  - ارجاع پرونده به واحد مالی و خزانه‌داری.
- **نقش‌های مجاز**: کارشناسان پشتیبانی (`support_agent`)، تاییدکنندگان مالی.

---

## ۶. ماژول مدیریت ذینفعان و تامین‌کنندگان (`VendorsView`)
- **هدف**: نگهداری دفترچه حساب‌های بانکی، شماره شبا، و مشخصات اقتصادی تامین‌کنندگان سازمان.
- **ویژگی‌ها**:
  - ثبت، ویرایش و جستجوی تامین‌کنندگان به تفکیک گروه‌ها (`VendorCategory`).
  - اعتبارسنجی شماره شبای بانکی (`shebaNumber`).
  - نمایش سابقه و حجم کل پرداختی‌های انجام شده به هر تامین‌کننده.
- **نقش‌های مجاز**: خزانه‌داری، ادمین.

---

## ۷. ماژول ساختار سازمانی و مراکز هزینه (`CompaniesView` & `CostCentersView`)
- **هدف**: مدیریت شرکت‌های هلدینگ، شعبه‌ها، و بودجه‌های مصوب ماهانه مراکز هزینه.
- **ویژگی‌ها**:
  - تعریف شرکت‌های تابعه و حساب‌های بانکی مرتبط.
  - تعریف مراکز هزینه و کنترل بودجه مصوب (`monthlyBudget`).
- **نقش‌های مجاز**: مدیر سیستم (`manage_cost_centers`, `manage_companies`).

---

## ۸. ماژول مدیریت کاربران و دسترسی‌ها (`AdminPanel` & `RolesAndPermissionsView`)
- **هدف**: کنترل کامل هویت پرسنل، تعریف نقش‌های سیستمی، پرمیشن‌های ۲۲‌گانه، مسیرهای تایید، و نقش دوگانه.
- **ویژگی‌ها**:
  - مدیریت حساب کاربری، تغییر رمز عبور و وضعیت فعال بودن.
  - تنظیم نقش دوگانه (`isDualRole`) و سرپرست ارشد خزانه‌داری (`isSeniorTreasurySupervisor`).
  - تعیین مقصدهای مجاز ارجاع (`allowedApproverIds`) و توالی مراحل تایید (`approvalChain`).
- **نقش‌های مجاز**: فقط مدیر سیستم (`admin`).
